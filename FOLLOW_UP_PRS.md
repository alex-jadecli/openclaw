# Follow-Up PR Ideas

Track follow-up work suggested by Claude Code sessions. Each entry links back to the
originating PR so context is never lost.

## How to use

1. When Claude Code creates a PR, it adds follow-up ideas to the **Follow-Up Ideas**
   section of the PR description.
2. Those same ideas are appended here under **Backlog** with a link to the source PR.
3. To start a new Claude Code session for an idea, copy the idea text and use it as your
   prompt. Claude Code will read this file for additional context.

## Backlog

<!-- Append new entries below this line. Format:
- [ ] **Idea title** — short description (source: #PR_NUMBER)
-->

- [ ] **Dedicated openclaw user setup scripts for macOS and WSL2** — Create a reproducible setup guide and/or script for bootstrapping a dedicated `openclaw` user on (1) macOS (MacBook) and (2) Windows 11 with WSL2 Ubuntu 26.04. Should cover: creating the local user account, installing Node 22+, pnpm, Claude Code CLI, cloning the repo, configuring Claude auth, and any OS-specific quirks (macOS `sysadminctl`/`dscl` vs WSL2 `adduser`, shell profile, SSH keys, permissions). Goal: a new Claude Code session can pick this up and walk the user through the full setup interactively. (source: PR from `claude/release-template-followup-ideas-z7Wub`)
- [ ] **Install and validate `/github-install-app` for Claude Code Actions integration** — Run `/github-install-app` (CLI-only slash command) to install the GitHub app required for Claude Code Actions (CI-triggered Claude Code runs). Validate the app is installed correctly on the `alex-jadecli/openclaw` fork, confirm it has the right repository permissions, and verify that Claude Code Actions can be triggered from PRs/issues. Document any setup steps or gotchas in CLAUDE.md. (source: PR from `claude/release-template-followup-ideas-z7Wub`)
- [ ] **Install Astral toolchain (uv, ty, ruff) with standalone Python** — Install Astral's standalone Python distribution as the base Python via `uv python install`, then set up the full Astral toolchain: `uv` (package/project manager), `ty` (type checker), and `ruff` (linter/formatter). Configure on both macOS and WSL2 environments. Should cover: installing uv via the official installer, pinning a Python version with `uv python pin`, setting up `ruff` and `ty` configs if needed, and verifying everything works (`uv run`, `ruff check`, `ty check`). This gives a clean, Homebrew/apt-free Python stack managed entirely by Astral's tools. (source: PR from `claude/release-template-followup-ideas-z7Wub`)
- [ ] **Configure releasebot.yaml for AI vendor release notifications** — Add a `releasebot.yaml` config to subscribe to release notifications via https://releasebot.io. Set all vendors to `false` by default, then enable (`true`) for AI-related vendors: Anthropic (https://releasebot.io/updates/anthropic), OpenAI, Google (Gemini), Meta (Llama), Mistral, Cohere, and any other relevant AI tooling vendors (e.g. Astral/uv, Vercel/Next.js). This keeps the team informed of new model releases, SDK updates, and breaking changes from the AI providers openclaw integrates with. (source: PR from `claude/release-template-followup-ideas-z7Wub`)
- [ ] **Set up cloud services with full Claude ecosystem integrations (Neon, MongoDB, Redis, Docker, Ollama, Airflow, Prefect)** — Create accounts and configure the deepest available Claude integration for each service _before_ writing any custom code. For each vendor, evaluate and enable the full spectrum: MCP servers, Claude Code plugins/skills, Docker MCP Catalog & Toolkit (https://docs.docker.com/ai/mcp-catalog-and-toolkit/toolkit/), native AI assistants, and any vendor-specific Claude partnerships. Services: (1) **Neon** — serverless Postgres, set up project + connection string, enable Neon's MCP server and any Claude Code plugin for natural-language queries, migrations, and schema management; (2) **MongoDB Atlas** — cloud cluster, configure the MongoDB MCP server, Claude Code plugin, or Atlas AI integration for collection/aggregation ops; (3) **Redis Cloud** — provision instance, set up the Redis MCP server and any Claude Code skill for cache/pub-sub management; (4) **Docker** — account + access tokens, set up Docker's MCP Catalog & Toolkit (https://docs.docker.com/ai/mcp-catalog-and-toolkit/toolkit/) for AI-assisted container management, image building, and compose orchestration via Claude; (5) **Ollama** — install and configure for local model inference, set up any MCP server or Claude Code integration for running/testing local models alongside Claude; (6) **Apache Airflow** (Astronomer or self-hosted) — set up workspace, configure any MCP server, Claude plugin, or AI-assisted DAG generation integration; (7) **Prefect Cloud** — workspace + API key, enable Prefect's MCP server, Claude integrations, and AI-assisted flow authoring. Goal: for each service, use the richest Claude integration available (plugin > skill > MCP server > native AI > REST API), only falling back to custom code when no integration covers the use case. Document credentials (via env vars, not committed), integration type used per vendor, and setup steps. (source: PR from `claude/release-template-followup-ideas-z7Wub`)
